import {

  AlertDialog
  // , Avatar, Badge, 
  ,Button 
  // ,ButtonGroup,
  // Card, Carousel, ContextMenu, DropdownMenu, Field, HoverCard,
  // Input, InputGroup, Item, Label, Progress, RadioGroup,
  // ResizableHandle, ResizablePanel, ResizablePanelGroup,
  // ScrollArea, Separator, Sheet, Sidebar, Skeleton,
  // Switch, Tabs, Textarea, ToggleGroup, Toggle, Tooltip
} from "../ui/index.tsx";

export default function Home() {
  return (
    <>
    <h1>
      RANDOM AAH TEXT 
    </h1>
    <Button>Click me</Button>
    <AlertDialog> RANDOM AAH TEXT </AlertDialog>
    </>
  );
}